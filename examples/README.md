# AIT Token Examples

Sample token payloads showing a complete AIT chain: a human authorizes a session, the agent modifies a file, then delegates to a child agent.

These are illustrative examples. Signatures are placeholder values.
